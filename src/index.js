// @flow

type S = { a: number }
var a: S = { a: 0 }

type X = number
var b: X = 0

var c

var d = {}

type Y = string
let e: Y = ''

type Z = {
	a: string,
	b: string,
}

type Z2 = { a: string, b: string }

const f = {
	a: 'this is a',
	b: 'this is b',
}
